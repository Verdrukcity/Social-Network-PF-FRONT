/**
 * objeto de las acciones de los categories
 */

const categoriesActions = {
  /**
   * función que retorna el estado global de categories
   */
  getAllCategories: (state) => state,
}

export default categoriesActions
